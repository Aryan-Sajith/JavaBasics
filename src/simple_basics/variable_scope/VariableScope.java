package simple_basics.variable_scope;

public class VariableScope {
    public static void main(String[] args) {
        //Variable Scope = The extent to which a variable is visible in your
        //program
        //
        //Local = Declared inside a method and visible only within said method
        //Global = Declared outside a method, but inside a class and visible
        //to all members of said class

        DiceRoller diceRoller = new DiceRoller();
    }
}
